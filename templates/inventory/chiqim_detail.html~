{% extends "index.html" %}
{% load static %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
<div class="container-fluid py-4">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h4 class="mb-0">{{ title }}</h4>
                    <div>
                        <a href="{% url 'chiqim_edit' outgoing.id %}" class="btn btn-warning btn-sm me-2">
                            <i class="fas fa-edit"></i> Tahrirlash
                        </a>
                        <a href="{% url 'chiqim_delete' outgoing.id %}" class="btn btn-danger btn-sm me-2">
                            <i class="fas fa-trash"></i> O'chirish
                        </a>
                        <a href="{% url 'chiqim_list' %}" class="btn btn-secondary btn-sm">
                            <i class="fas fa-arrow-left"></i> Orqaga
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    <!-- Asosiy ma'lumotlar -->
                    <div class="row mb-4">
                        <div class="col-md-6">
                            <table class="table table-bordered">
                                <tr>
                                    <th width="30%">Raqam:</th>
                                    <td>#{{ outgoing.id }}</td>
                                </tr>
                                <tr>
                                    <th>Sana:</th>
                                    <td>{{ outgoing.date|date:"d.m.Y" }}</td>
                                </tr>
                                <tr>
                                    <th>Ombor:</th>
                                    <td>{{ outgoing.warehouse.name }}</td>
                                </tr>
                                <tr>
                                    <th>Xaridor:</th>
                                    <td>{{ outgoing.customer.name|default:"Noma'lum xaridor" }}</td>
                                </tr>
                                <tr>
                                    <th>Yaratdi:</th>
                                    <td>{{ outgoing.created_by.get_full_name|default:outgoing.created_by.username }}</td>
                                </tr>
                                <tr>
                                    <th>Yaratilgan vaqt:</th>
                                    <td>{{ outgoing.created_at|date:"d.m.Y H:i" }}</td>
                                </tr>
                            </table>
                        </div>
                        <div class="col-md-6">
                            <table class="table table-bordered">
                                <tr>
                                    <th width="30%">Jami summa:</th>
                                    <td class="text-success fw-bold">{{ outgoing.total_amount|floatformat:2 }} so'm</td>
                                </tr>
                                <tr>
                                    <th>To'langan:</th>
                                    <td class="text-primary">{{ outgoing.paid_amount|floatformat:2 }} so'm</td>
                                </tr>
                                <tr>
                                    <th>Qarz:</th>
                                    <td class="{% if outgoing.debt > 0 %}text-danger{% else %}text-success{% endif %}">
                                        {{ outgoing.debt|floatformat:2 }} so'm
                                    </td>
                                </tr>
                                <tr>
                                    <th>Foyda:</th>
                                    <td class="text-info fw-bold">{{ outgoing.profit|floatformat:2 }} so'm</td>
                                </tr>
                                {% if outgoing.note %}
                                <tr>
                                    <th>Izoh:</th>
                                    <td>{{ outgoing.note }}</td>
                                </tr>
                                {% endif %}
                            </table>
                        </div>
                    </div>

                    <!-- Mahsulotlar ro'yxati -->
                    <h5 class="mb-3">Mahsulotlar ro'yxati</h5>
                    {% if items %}
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered">
                            <thead class="table-dark">
                                <tr>
                                    <th>#</th>
                                    <th>Mahsulot nomi</th>
                                    <th>Miqdor</th>
                                    <th>Narx</th>
                                    <th>Jami</th>
                                    <th>Tan narxi</th>
                                    <th>Foyda</th>
                                    <th>Partiya</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for item in items %}
                                <tr>
                                    <td>{{ forloop.counter }}</td>
                                    <td>
                                        {% if item.product %}
                                            <strong>{{ item.product.name }}</strong><br>
                                            <small class="text-muted">{{ item.product.warehouse.name }}</small>
                                        {% else %}
                                            <span class="text-muted">Mahsulot o'chirilgan</span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        <span class="badge bg-info">{{ item.quantity }}</span>
                                        {% if item.product %}
                                            {{ item.product.unit }}
                                        {% endif %}
                                    </td>
                                    <td>{{ item.price|floatformat:2 }} so'm</td>
                                    <td class="fw-bold">{{ item.total|floatformat:2 }} so'm</td>
                                    <td>{{ item.cost_price|floatformat:2 }} so'm</td>
                                    <td class="{% if item.profit > 0 %}text-success{% elif item.profit < 0 %}text-danger{% else %}text-muted{% endif %}">
                                        {{ item.profit|floatformat:2 }} so'm
                                    </td>
                                    <td>
                                        {% if item.batch_number %}
                                            <small>{{ item.batch_number }}</small>
                                        {% else %}
                                            <small class="text-muted">-</small>
                                        {% endif %}
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                            <tfoot class="table-light">
                                <tr>
                                    <th colspan="4" class="text-end">Jami:</th>
                                    <th>{{ outgoing.total_amount|floatformat:2 }} so'm</th>
                                    <th>-</th>
                                    <th>{{ outgoing.profit|floatformat:2 }} so'm</th>
                                    <th>-</th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    {% else %}
                    <div class="alert alert-info">
                        <i class="fas fa-info-circle"></i> Bu chiqimda mahsulotlar mavjud emas.
                    </div>
                    {% endif %}

                    <!-- Qo'shimcha ma'lumotlar -->
                    {% if outgoing.invoice_number %}
                    <div class="row mt-4">
                        <div class="col-md-6">
                            <div class="alert alert-secondary">
                                <strong>Faktura raqami:</strong> {{ outgoing.invoice_number }}
                            </div>
                        </div>
                    </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <!-- Print tugmasi -->
    <div class="row mt-3">
        <div class="col-12 text-center">
            <button class="btn btn-primary" onclick="window.print()">
                <i class="fas fa-print"></i> Chop etish
            </button>
        </div>
    </div>
</div>

<style>
@media print {
    .btn, .card-header .btn, .no-print {
        display: none !important;
    }
    
    .card {
        border: none !important;
        box-shadow: none !important;
    }
    
    .container-fluid {
        padding: 0 !important;
    }
}
</style>
{% endblock %}